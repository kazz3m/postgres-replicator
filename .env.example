# Optional: pre-fill DSNs (can also be set via UI)
SOURCE_DSN=postgresql://user:pass@source-host:5432/dbname
DEST_DSN=postgresql://user:pass@dest-host:5432/dbname

# Optional: custom path for the encryption key (default: /data/secret.key)
# SECRET_KEY_PATH=/data/secret.key
